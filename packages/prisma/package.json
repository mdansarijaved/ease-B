{
  "name": "@my-turborepo/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "bunx prisma generate --watch",
    "build": "bunx prisma generate",
    "db:push": "bunx prisma db push --skip-generate",
    "db:migrate": "bunx prisma migrate dev",
    "db:deploy": "bunx prisma migrate deploy --preview-feature"
  },
  "devDependencies": {
    "prisma": "^5.15.0",
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0"
  }
}
